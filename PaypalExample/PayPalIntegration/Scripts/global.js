

/*Configura su servidor para hacer llamadas a PayPal.
Configura su servidor para recibir una llamada del cliente.
Llama a PayPal para crear un pedido.
Maneja cualquier error de la llamada.
Devuelve una respuesta exitosa al cliente con la ID del pedido.*/





function Testing(url) {
    window.location = url;
}

function WriteConsole(value) {
    console.log(value);
}